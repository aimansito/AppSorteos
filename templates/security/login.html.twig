{% extends 'base.html.twig' %}

{% block title %}Log in!{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <style>
        .full-width-form-container {
            max-width: 800px;
            margin: 40px auto; 
            padding: 20px;
        }
            
        .form-check-input {
            width: 1.25em;
            height: 1.25em;
            margin-right: 0.5rem;
        }
        
        .form-check-label {
            padding-top: 2px;
            font-size: 1.1rem;
        }
    </style>
{% endblock %}

{% block body %}
<div class="full-width-form-container">
<form method="post">
    {% if error %}
        <div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
    {% endif %}

    {% if app.user %}
        <div class="mb-3">
            Ya estás logueado como {{ app.user.userIdentifier }}, <a href="{{ path('app_logout') }}">Cerrar sesión</a>
        </div>
    {% endif %}
        <h1 class="h3 mb-3 font-weight-normal">Por favor, inicia sesión</h1>
        <label for="inputUsername">Nombre de usuario</label>
        <input type="text" value="{{ last_username }}" name="username" id="inputUsername" class="form-control" autocomplete="username" required autofocus>
        <label for="inputPassword">Contraseña</label>
        <input type="password" name="password" id="inputPassword" class="form-control" autocomplete="current-password" required>

    <input type="hidden" name="_csrf_token" data-controller="csrf-protection"
        value="{{ csrf_token('authenticate') }}"
    >

        <div class="mb-3 mt-3"> 
            <div class="form-check">
                <input type="checkbox" name="_remember_me" id="remember_me_input" class="form-check-input"> 
                <label class="form-check-label" for="remember_me_input"> 
                    Recuérdame
                </label>
            </div>
        </div>

        <button class="btn btn-lg btn-primary" type="submit">
            Iniciar sesión
        </button>
    </form>
    </div>
{% endblock %}